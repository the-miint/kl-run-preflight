# Native test fixtures are generated by scripts/generate_native_test_files.py and
# regenerated wholesale on any schema change (a bumped user_version alone touches
# every snapshot). They carry no hand-reviewable content, so mark them generated
# — GitHub collapses them in diffs/PR review — and binary where applicable.
tests/data/native/** linguist-generated=true
tests/data/native/*.sqlite binary
